apiVersion: apps/v1
kind: Deployment
metadata:
  name: log-output
spec:
  replicas: 1
  selector:
    matchLabels:
      app: log-output
  template:
    metadata:
      labels:
        app: log-output
    spec:
      volumes:
        - name: shared-status
          emptyDir: { }
      containers:
        - name: log-writer
          image: dazakharova/log_output:1.3
          command: [ "node", "writer.js" ]
          volumeMounts:
            - name: shared-status
              mountPath: /usr/src/app/shared
        - name: log-reader
          image: dazakharova/log_output:1.3
          volumeMounts:
            - name: shared-status
              mountPath: /usr/src/app/shared
          env:
            - name: PORT
              value: "3000"