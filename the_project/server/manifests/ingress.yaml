apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-server
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: todo-server-svc
                port:
                  number: 2345